[project]
name = "xunoia_whitelabel"
authors = [
    { name = "Xunoia Technologies Private Limited", email = "engineering@xunoia.com" }
]
description = "Customer-facing white-label branding for Frappe Framework + ERPNext v16."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
